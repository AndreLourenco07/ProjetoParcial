<?xml version="1.0" encoding="utf-8"?>
<!--
Este é o layout para um único item na grade da RecyclerView.
- CardView cria um card com elevação e cantos arredondados, seguindo o Material Design.
- ImageView mostrará a imagem da lista ou um placeholder.
- TextView mostrará o título da lista.
-->
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    app:cardCornerRadius="12dp"
    app:cardElevation="4dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <!-- Imagem da lista -->
        <ImageView
            android:id="@+id/imageViewList"
            android:layout_width="match_parent"
            android:layout_height="140dp"
            android:scaleType="centerCrop"
            android:background="#E0E0E0"
            tools:srcCompat="@drawable/ic_placeholder_image"
            android:contentDescription="Imagem da lista"/>

        <!-- Título da lista -->
        <TextView
            android:id="@+id/textViewListTitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="12dp"
            android:gravity="center_vertical"
            android:textSize="16sp"
            android:textStyle="bold"
            android:textColor="@android:color/black"
            tools:text="Supermercado" />

    </LinearLayout>

</com.google.android.material.card.MaterialCardView>